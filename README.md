# Email account recovery demo

### Setup instructions
- Move `.env.example` to `.env` and update the value of `PIMLICO_API_KEY`.
- Run `yarn` to install the dependencies and then `yarn dev` to start the server.
